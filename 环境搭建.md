# 环境搭建与协作指南

《断线之后》· 微信小游戏 · 5 人实训项目

---

## 一、硬件要求

| 项 | 最低 | 推荐 | 说明 |
|---|---|---|---|
| 系统 | Windows 10+ / macOS | — | Cocos Creator 与微信开发者工具均支持，两端操作一致 |
| 内存 | 8 GB | **16 GB** | Cocos Creator + 微信开发者工具 + 浏览器同时开，8GB 会明显卡顿 |
| 磁盘 | 20 GB 可用 | 50 GB | 引擎、工程缓存、真机调试包都占空间 |

**真机测试不需要额外设备。** 微信开发者工具的「预览」功能会生成二维码，用手机微信扫码即可在真机上运行，安卓和 iPhone 都支持。

---

## 二、软件清单

### 全员必装

| 软件 | 用途 | 下载 |
|---|---|---|
| **Git** | 版本控制 | git-scm.com |
| **Cocos Dashboard + Cocos Creator 3.x** | 游戏编辑器，从 Dashboard 里下载 Creator | cocos.com |
| **微信开发者工具** | 真机预览、上传体验版 | 微信官方文档 → 工具 → 下载 |

> Cocos Creator 具体版本在 **9/22 技术路线冻结**时确定，全组必须统一，否则工程打不开。

### 按成员细分

| 成员 | 必装 | 用途 |
|---|---|---|
| **A** | Cocos Creator、绘图工具、微信开发者工具、Office/WPS | 出美术资产；在编辑器里预览验证触点位置 |
| **B** | 同上 | 同上 |
| **C** | 微信开发者工具、Node.js、VS Code、Office/WPS | 后端开发；上传体验版；写需求文档 |
| **D** | Cocos Creator、微信开发者工具、Node.js、VS Code | 关卡开发与真机调试 |
| **E** | Cocos Creator、微信开发者工具、Node.js、VS Code | 外层页面开发与真机调试 |

**绘图工具**：Photoshop / Illustrator / Figma / Procreate 都行，免费替代是 Krita、GIMP。

**VS Code**：Cocos Creator 内置的脚本编辑器就是 VS Code 内核，但独立装一个更方便写后端和看 diff。

**不想用命令行 Git 的**（主要是 A、B）：可以装 **GitHub Desktop**，图形化操作，但推送前仍需配好 SSH。

---

## 三、账号与权限清单

| 账号 / 权限 | 谁负责开通 | 谁需要使用 | 说明 |
|---|---|---|---|
| **微信公众平台小游戏账号** | C | C | 个人主体，需本人身份证 + 实名微信号。开通后拿到 **AppID** |
| **微信云开发环境** | C | C、D、E | 在小程序后台开通，需把 D、E 加为**环境成员** |
| **GitHub 账号** | 各自 | 全员 | — |
| **仓库 Collaborator** | 仓库属主 | 全员 | 仓库 Settings → Collaborators → 添加。**不加就没有推送权限** |
| **SSH 密钥** | 各自 | 全员 | 见第五节。不配的话推送会失败 |
| **微信开发者工具登录** | 各自 | 全员 | 微信扫码登录 |
| **体验成员名单** | C | 全员 + 老师 | 上限 15 人。只有名单内的人能扫体验版二维码 |

**AppID 是整条链路的起点**——没有它，微信开发者工具建不了项目，云开发环境也开不了。C 要在 9/22 前拿到。

---

## 四、协作流程

### 日常开发

```bash
git switch main
git pull                      # 先同步

git switch -c feat/level-01   # 开短分支
# ……编写代码……
git add -A
git commit -m "feat(level): 接入第1关热点点击 (FR-07)"
git push -u origin feat/level-01

# 到 GitHub 开 Pull Request，找一个人 review 后合并
# 合并后清理本地分支
git switch main && git pull
git branch -d feat/level-01
```

分支寿命控制在 1~3 天。谁改哪个文件见根目录 `README.md` 的目录所有权表。

### 关卡交接链路

这是本项目最关键的一条流水线，每个关卡都要走一遍：

| 步骤 | 负责 | 交付物 |
|---|---|---|
| 1. 写关卡开发卡 | A / B | 剧情、双视角线索、触点、标准答案、图片清单 |
| 2. 出美术资产 | A / B | 原图 + 导出图 + 触点坐标表 |
| 3. 接入关卡 | **D** | 关卡配置 JSON + TypeScript 脚本 |
| 4. 挂地图入口 | **E** | 地图节点、解锁条件 |
| 5. 单 / 双人测试 | 全员 | 真机走通，记录测试结果 |

A、B 交资产给 D，D 交关卡给 E 挂入口——**顺序不能乱**，这也是《项目分工》里依赖交接表的内容。

---

## 五、首次配置：SSH 密钥

**每个人都要做**，否则推送会失败。

在 **Git Bash**（Windows）或**终端**（macOS）中执行：

```bash
# 1. 生成密钥（已有则跳过）
ssh-keygen -t ed25519 -C "你的邮箱"

# 2. 复制公钥内容
cat ~/.ssh/id_ed25519.pub

# 3. 粘贴到 GitHub：Settings → SSH and GPG keys → New SSH key

# 4. 验证（出现 Hi 你的用户名! 就成功了）
ssh -T git@github.com

# 5. 克隆时用 SSH 地址，不要用 https
git clone git@github.com:yaya11111111/BeiJiaoGame.git
```

> `~/.ssh/` 对应的实际路径：Windows 是 `C:\Users\你的用户名\.ssh\`，macOS 是 `/Users/你的用户名/.ssh/`。
> **Windows 用户请在 Git Bash 里操作**（安装 Git 时自带），命令与文档完全一致。

已经用 https 克隆过的，改一下远程地址即可：

```bash
git remote set-url origin git@github.com:yaya11111111/BeiJiaoGame.git
```

### 网络说明

**GitHub 在校园网下是间歇性可达的**——实测过 `github.com:443` 有时卡满 75 秒超时，有时 2 秒就通。推送失败时先重试，不要急着怀疑配置。

---

## 六、9/22 技术路线冻结前的准备清单

| 事项 | 负责 | 通过标准 |
|---|---|---|
| 拿到 AppID | C | 微信开发者工具能新建项目 |
| 开通云开发环境 | C | D、E 被加为环境成员 |
| 统一 Cocos Creator 版本 | C、D | 全组装同一个版本，工程能互相打开 |
| 真机打开空白游戏页 | C、D | 构建 → 上传 → 扫码，看到空白页 |
| 全员 SSH 配好 | 各自 | `ssh -T git@github.com` 返回成功 |
| 全员加为 Collaborator | 仓库属主 | 每人都能 push |
| 包体基线实测 | C、D | 裁剪后引擎占用不超首包预算 |
| 云开发 `watch` 实时同步验证 | C | 双账号双机，消息延迟可接受 |
| 公共 prefab 清单与归属 | D、E | 每个 prefab 有唯一负责人 |
| 关卡配置字段冻结 | D、C、E | `levelId` / `viewId` / `assetKey` / `nodeId` / `progress` |
