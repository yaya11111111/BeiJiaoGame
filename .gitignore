# ============================================================
# 本地文档 —— 不进版本库
# 需求评审、产品详细设计、选型报告、项目分工等内部文档只保留在本地。
# 注意：本规则使整个 docs/ 目录被忽略；若要放回某份文档，用
#       git add -f docs/某文件.md 强制加入。
# ============================================================
docs/

# ============================================================
# Cocos Creator —— 这几个目录绝对不能提交！
# library/ 是编辑器生成的资源缓存，几千个文件且每次打开都会变，
# 一旦提交，仓库几周内就能涨到几个 G。build/ 是导出产物，同样不入库。
# ============================================================
library/
temp/
local/
build/
profiles/
native/

# ============================================================
# Node.js（server/ 目录）
# ============================================================
node_modules/
dist/
*.log
npm-debug.log*
yarn-error.log*

# ============================================================
# 环境变量 / 密钥 —— 云开发的 secretId、数据库连接串等一律不进仓库
# ============================================================
.env
.env.local
.env.*.local

# ============================================================
# 微信开发者工具
# project.private.config.json 含本机私有配置（AppID、路径），不同人不一样
# ============================================================
project.private.config.json
.wechat/

# ============================================================
# 编辑器 / 系统
# ============================================================
.DS_Store
.idea/
.vscode/
*.swp

# ============================================================
# 根目录的截图 —— 不入库
# 前导斜杠表示只匹配仓库根目录。去掉斜杠会递归匹配，
# 把 client/assets/textures/ 下 A/B 的场景图一起挡掉。
# ============================================================
/*.png
/*.jpg
